{
    "name": "my-app",
    "private": true,
    "scripts": {
        "dev": "concurrently \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\""
    },
    "devDependencies": {
        "concurrently": "^8.0.0"
    },
    "dependencies": {
        "@prisma/client": "^6.9.0"
    }
}
